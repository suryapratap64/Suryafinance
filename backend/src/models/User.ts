// Sequelize User model removed for Supabase migration. Use Supabase client for user operations.

/*

Exactly! In Supabase, you don’t need models like in Sequelize or TypeORM because Supabase is essentially a PostgreSQL database with a JS client. Here’s a concise breakdown for your README or notes:

Supabase → No ORM Models Needed

Supabase provides supabaseClient / supabaseAdmin to query tables directly.

Tables are already defined in the database, so you don’t create JS/TS models.

Example of inserting a row:
*/
